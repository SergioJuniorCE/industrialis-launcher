{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@industrialis-launcher/api": "workspace:*",
    "@industrialis-launcher/auth": "workspace:*",
    "@industrialis-launcher/db": "workspace:*",
    "@industrialis-launcher/env": "workspace:*",
    "@orpc/openapi": "catalog:",
    "@orpc/server": "catalog:",
    "@orpc/zod": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@industrialis-launcher/config": "workspace:*",
    "@types/bun": "^1.3.4",
    "tsdown": "^0.21.9",
    "typescript": "catalog:"
  }
}
